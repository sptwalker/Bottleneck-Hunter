# 周期性自动运行守卫加固 — 修复与验收日志

- **日期**：2026-09-06
- **提交**：`9590a0c`（已 push origin/main）
- **范围**：全系统 34 个 APScheduler 周期任务的「静默失败」守卫缺口
- **验收原则**：真实生产数据证明，不靠代码臆测

## 一、缺口修复清单

| # | 缺口 | 根因 | 修复 | 文件 |
|---|------|------|------|------|
| 1 | 日常决策/每周策略 `had_fatal` 被自身 except 吞掉 → 心跳恒 success，守卫「据 error 补跑」形同虚设 | per-user try 内 raise 被本函数 except 捕获 | `fatal_users` 循环外累积，末尾统一 raise `RuntimeError(...无可用 LLM...)`；保留用户隔离 | `watchlist/scheduler.py` (`job_daily_decision` / `job_weekly_strategy`) |
| 2 | 重启若跨过 job 触发点 → 当次漏跑，须等次日 07:45 守卫 | 无开机即扫 | 启动补跑注册 `system_watchdog_startup`（+90s，复用现有 watchdog 扫超期/interrupted 心跳→主动补跑） | `web/app.py` lifespan |
| 3 | 宏观采集真异常被吞成 success，守卫永远看不到已坏 | except 无 re-raise | 真异常 re-raise 置 error 心跳（软失败已有缓存兜底，抵达此处必是无缓存真异常） | `watchlist/scheduler.py` (`job_macro_update`) |
| 4 | 邮件轮询 IMAP 认证失败每 interval 恒 error → 守卫无意义反复补跑 | 未配置的账户被当故障 | 持久认证类错误（login/auth/password/abnormal/not open/535/invalid…）静音跳过 success，不触发补跑 | `watchlist/scheduler.py` (`job_poll_imap`) |
| 8 | 委员会裸壳同步 invoke 无 asyncio 级超时 → 单模型 hang 拖死整个决策周期 | 无硬超时上限 | `asyncio.wait_for(to_thread(invoke), timeout=_CAND_TIMEOUT+30)` | `watchlist/committee.py` |

### 刻意不修（documented）
- **缺口5** 守卫自排除：容器 healthcheck（/healthz 探调度器存活）已覆盖，YAGNI。
- **缺口6** interval 任务重启计时器漂移：APScheduler 语义正常，非缺口。
- **缺口7** 委员会轻微 over-accounting：刻意保留手动 `record_failure` 以覆盖 wait_for 超时（裸壳后台线程尚未记账的窗口），「超时必记」优先于零重复。

## 二、全量测试

`python -m pytest -q` → **1523 passed, 4 skipped**（基线 1519 + 4 条新回归测试，零回归）。

新增回归测试（`tests/test_scheduler_jobs.py`）：
- `test_had_fatal_raises_for_error_heartbeat`（daily / weekly 各一）— had_fatal 必抛错
- `test_had_fatal_preserves_user_isolation` — 首个用户 fatal 不中断，`drain.await_count==2`
- `test_all_ok_no_raise` — 全正常不抛错

## 三、部署机制关键发现

**生产容器把源码 bake 进镜像，只 bind-mount `data`/`.env`/`output`。`git reset --hard` + `docker restart` 不上线代码。**
正确部署 = 服务器根目录跑 `./deploy.sh`（git pull --ff-only + `docker compose ... up -d --build` 重建镜像 + /healthz 检查）。
已存为长期记忆 `project_prod_deploy_requires_rebuild`。

本次 `deploy.sh` 结果：镜像重建 `sha256:ffcb35df…`，容器 Recreated，healthz OK(200)，HEAD=9590a0c。

## 四、真实生产数据验收

### 代码证真（容器内 grep）
```
grep -c system_watchdog_startup /app/.../web/app.py        → 1
grep -c fatal_users            /app/.../watchlist/scheduler.py → 8
grep -c wait_for               /app/.../watchlist/committee.py → 4
```
镜像 Created 2026-09-06T18:33 CST，容器 health=healthy。

### 运行时可观测证据（生产日志）
- **缺口2**：`Added job "System watchdog (startup catch-up)"` @18:33:12 → **触发** @18:34:42。
- **缺口4**：watchdog 触发的邮件轮询命中真实 IMAP 错误
  `b'Login fail. Account is abnormal, service is not open, password is incorrect...'`
  → 打印新增日志「IMAP 认证/账户异常…静音跳过（不触发守卫补跑）」→ **未**触发补跑。

### 心跳健康（`pipeline_status` 只读 mode=ro）
- **34/34 job 心跳 = success，error/interrupted = 0**。
- `job:mail_ingest_poll`：**error → success**（2026-09-06T10:34:43，即上述静音跳过那次）——缺口4 闭环。
- `job:system_watchdog` / `system_watchdog_startup` 均已运行，未标记任何 job 超期。

## 结论

5 项缺口修复全部上线并经真实生产数据验证；3 项刻意不修有据；全量测试零回归。系统周期性自动运行的静默失败守卫已闭环：**真故障必置 error 心跳 → 守卫补跑；未配置项静音跳过不空转；单模型 hang 有硬超时；重启漏跑开机即补。**
